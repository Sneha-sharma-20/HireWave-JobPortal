<h2>Available Jobs</h2>

<% if @jobs.count == 0 %>
  <div class="card">
    <p>No jobs available at the moment. Check back later!</p>
  </div>
<% else %>
  <% @jobs.each do |job| %>
    <div class="job-card">
      <h3><%= job['title'] %></h3>
      <div class="company"><%= job['company'] %></div>
      <div class="meta">
        <% if job['location'] %>
          📍 <%= job['location'] %>
        <% end %>
        <% if job['salary'] %>
          | 💰 <%= job['salary'] %>
        <% end %>
        <% if job['job_type'] %>
          | 💼 <%= job['job_type'] %>
        <% end %>
      </div>
      <div class="description">
        <%= job['description'][0..200] %><%= job['description'].length > 200 ? '...' : '' %>
      </div>
      <a href="/jobs/<%= job['id'] %>" class="btn">View Details</a>
    </div>
  <% end %>
<% end %>